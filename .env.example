# Copy to .env and fill in. Nothing in this template requires these by default;
# they exist for experiments that pull data from external systems.

# Path to an alternative config file used by the training/prediction CLIs.
ML_CONFIG_PATH=config/config.yaml

# Credentials for external data sources, if your experiment needs them.
# DATABASE_URL=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
